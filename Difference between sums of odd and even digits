num = int(input("Enter a number: "))
oddsum = 0
evensum = 0
while num > 0:
    digit = num % 10
    if digit % 2 == 0:
        evensum += digit
    else:
        oddsum += digit
    num = num // 10
difference = oddsum - evensum
print("Difference:", difference)
